# Implementation status

## Review fixes and efficiency work (version 0.5.0, September 20)

- All eight reviewed defects are repaired. Added safe worker imports, strict weather timestamps, hemisphere-aware imported defaults, patched Vite, finite zero-DLI rendering, early grid budgets, live material refresh and physically bounded saved results/checkpoints.
- Implemented single-pass parsing, period backend/geometry reuse, 2 MiB diffuse tiles, byte-aware LRU caches, bounded crop queries, hardware instancing, debounced preferences with IndexedDB weather, shared export geometry and lazy export loading. Unsaved-layout warnings and WebGL recovery are included. Numerical model revision invalidates older calculated results while preserving imported studies.
- Final verification: **141 tests pass**, build and formatting pass, live audit reports **zero vulnerabilities**, and all **480 archived files are unchanged**. Main chunk is 492.83 kB / 157.18 kB gzip. Actual Chromium CPU/WebGPU, tiled transmission, zero-DLI exports/storage and context recovery checks pass. Native Radiance first-hit and transmission comparisons pass.
- Expanded scientific evidence: 288 pvlib solar comparisons, 11 valid independent Radiance sky comparisons (one oracle error sky explicitly excluded), six daily comparisons across both hemispheres, 198 convergence solves and 12 dense winter sampling solves. New cross-browser/hardware CI is configured but has not run here. Measured field/PAR calibration needs a supplied dataset; physical browser/device and upper-limit annual coverage remain incomplete.
- See [the implementation and validation report](REVIEW-FIXES-2026-09-20.md) for the closure matrix, controlled performance measurements, exact scientific scope and external validation requirements. No production deployment was made.

## Historical project review (September 20; superseded by fixes above)

- Added [the project review](REVIEW-2026-09-20.md) for commit `e549b7a`, covering scientific calculations, state/import handling, rendering, field layouts, exports, dependencies, performance and deployment. The eight findings included weather-upload lost edits, timezone parsing, vulnerable development Vite, zero-DLI rendering, weather-import hemisphere defaults, oversized-grid guards, stale module textures and insufficient saved-result physical checks. The review itself did not change application code; the implementation below is now complete as recorded in the review-fixes report.
- Recorded local measurements for annual CSV parsing, repeated analysis-key serialization and crop-summary work. Recommended period-engine reuse, tiled transmission, bounded cache eviction, hardware instancing and more efficient persistence/export work. The review distinguishes new measurements from the existing September 11 capacity evidence.
- Reran the convergence harness with current racking defaults: 132 CPU solves / 44 sweeps completed. Largest absolute mean-sunlight differences from each sweep's finest reference were 0.0792 percentage points for sky patches, 0.1013 for direct time steps, 0.7125 for receiver spacing and 0.0011 for diffuse pose bins. These are small-array sensitivity results, not per-cell accuracy bounds. The refreshed JSON supersedes the earlier file while historical status entries retain their original observations.
- Verification: all 128 tests, production build, formatting and archive verification pass. The main bundle retains a size warning. Installed Radiance comparisons report zero first-hit mismatches over 51,100 rays / 10 cases and a maximum scalar-transmission difference of 1.11 × 10⁻¹⁶ over 32 rays / 8 combinations. Actual Chromium CPU/WebGPU comparisons pass for all five rack types, including transmitting-module cases, device-loss/allocation fallback and cache-storage failure; a small 366-day CPU browser solve also passes. The live dependency audit reports one high-severity vulnerable dev dependency (Vite), with no production dependency vulnerability reported. Independent full-model/field accuracy and broad browser/capacity validation remain release gates.

## Racking orientation defaults (September 20)

- Fixed tilt defaults to south-facing in the northern hemisphere and north-facing in the southern hemisphere; the equator uses south-facing. Both trackers and vertical bifacial default to a 90° reference bearing with north–south rows. Single-axis panels tilt east/west around those rows; dual-axis panels start with an east-facing preview and follow the sun in both tilt and azimuth during analysis. Vertical module faces point east/west. Pergola modules remain horizontal with east–west rows by default.
- Switching rack types carries standard bearings to the new type; nonstandard bearings are retained. Changing latitude, manually or through place selection, updates an existing standard fixed-tilt bearing across hemispheres. Geometry/clearance edits and saved/imported studies retain their existing bearing. The new **Apply default orientation** action explicitly updates an existing study. Orientation changes participate in the existing numerical-result invalidation and field-grid normalization paths.
- Controls, drawing callouts, methods tables and embedded export provenance distinguish module-facing direction, tracker preview/reference direction, row/rotation axis and horizontal pergola layout. The existing geometry/solver coordinate convention is unchanged; no automatic rotation of imported projects is introduced.
- Verification: all 128 tests, production build, formatting and frozen-archive verification pass. New tests check rack transitions, hemisphere changes, custom/imported bearings, actual module normals and row/grid coordinates, morning/evening sun directions, arbitrary layout bearings, and publication consistency. The simulated-DOM workflow verifies rack defaults and explicit reset; browser checks verify the reset, single-axis plan orientation, both tracker labels, vertical faces and northern fixed tilt. The installed Radiance harness uses the new default bearings and east/west morning/afternoon directions: zero occlusion mismatches over 51,100 rays / 10 cases. This remains first-hit occlusion validation, not independent daily-energy, field or WebGPU validation.

## Proprietary licensing (September 18)

- Added a root proprietary license requiring written permission for duplication, redistribution, modification, separate hosting and commercial use. It permits noncommercial use of the official browser application and sharing research exports, and preserves third-party licenses, frozen-archive rights, prior grants, statutory exceptions and GitHub platform rights.
- README documents the terms and identifies local setup as authorized development; package metadata and its lockfile point to `LICENSE`. Application and solver code are unchanged.
- Verification: `npm test`, `npm run build`, and `npm run archive:verify` pass. All 480 archived files match their recorded checksums. No new scientific validation is claimed for this documentation and metadata change.

## Bulk sensor selection and explicit crop-row defaults (September 18)

- **Add sensors** sits beside **Add crop beds** in both field-planning workspaces. Its modal defaults to 3 rows across each cropping area's width and 10 columns along its length, with every cell unchecked. Users choose a sensor type and individual cells, or select/clear a crop row or the entire array. Changing grid dimensions clears the selection; reopening starts empty and remembers the last applied type. The single-item tools retain their placement/drag behavior and stay grouped on the right.
- Each checked planning cell adds one independent physical sensor at its centre snapped to the existing receiver cell. The popup reports shared receiver locations when a fine planning grid maps several sensors to the same cell. Rotation, unequal aisle widths, installation heights, unique IDs across both fields, control treatment, export round trips and one-step undo are preserved. Sensor additions do not change the numerical grid, rerun irradiance or reset the camera. The UI explains the 500-sensor field limit and bounded preview size; invalid batches are rejected before editing.
- **Add crop beds** now opens with every crop row unselected. Add remains disabled until the user chooses a row.
- Verification: 123 tests pass, including rotated/aisle placement, all instrument types, cell collisions, limits, control independence and project round trips; the simulated-DOM workflow checks empty defaults, dimension-change clearing, type selection, adding/undoing a batch and no extra solve. Browser checks confirmed the popup layout, selected-cell placement across crop rows, crop defaults and modal help. Production build, formatting and frozen-archive verification pass. The installed Radiance comparison reports zero mismatches over 51,100 rays / 10 cases; its scope remains first-hit occlusion, not independent field, sky or GPU validation.

## Bulk crop-bed layouts and cropping-area references (September 16)

- **Add crop beds** opens a modal with a bed count, catalog crop picker, and keyboard-accessible top-down crop-row selector. Selected cropping areas are divided into equal-width, full-length beds, including wider group aisles. Existing beds are retained; each batch is one undoable edit. **Add sensor** and **Add single crop bed** sit on the right and retain their placement/drag behavior.
- Bulk beds use explicit `gridMode: exact` with fractional receiver coordinates so their edges match the cropping area without changing the numerical grid or invalidating irradiation. Single beds and sensors retain whole-cell snapping. Exact beds rotate with the array, support metre-based editing and corner resizing, copy to Control, and round-trip in project exports. Geometry/grid edits retain and clamp their grid coordinates, like other field layouts. Light summaries weight intersected receiver values by overlap area; this assumes uniform light within each receiver cell and adds no new irradiance samples. Methods and CSV describe the distinction. The existing 200-bed and 100 m per-dimension limits are checked before applying a batch.
- Cropping area is enabled when entering Agrivoltaic and Control planning. Control retains the same cropping rectangles as an optional visual reference while omitting PV infrastructure and reserved strips; the reference is toggleable and included in figure exports when enabled.
- Verification: 119 tests pass, including exact rotated/aisle partitions, partial-cell light conservation, validation limits, resizing, copy/move stability, project round trips, control reference exports, and the simulated-DOM create/undo workflow with no extra solve or camera-view reset. Browser checks covered row selection, changing the bed count, applying bulk layouts, exact-size editors and visible control cropping areas. Production build, formatting and frozen-archive verification pass. The installed Radiance comparison reports zero mismatches over 51,100 rays / 10 cases; this remains a first-hit occlusion comparison, not independent field, sky or GPU validation.

## Receiver boundary before irradiance calculation (September 15)

- The Irradiance stage enables the Receiver boundary layer by default before and after calculation. It shows the numerical field footprint, including the receiver buffer, in both the interactive view and SVG output. The existing grid button controls internal grid lines; turning those off retains the outer boundary. The boundary layer remains user-toggleable, and sensor/crop placement still begins in the field-layout stages.
- Verification: 114 tests pass, including the pre-calculation layer state and boundary-only plan/ortho exports. Production build and frozen-archive verification pass. The unchanged Radiance oracle comparison again reports zero mismatches over 51,100 rays / 10 cases; this is a display-only change.

## Engineering callout geometry and placement (September 15)

- Callout clearance now uses the exact convex hull of hardware vertices, computed once per geometry change and compacted in bounded batches. World-axis and array-aligned bounding boxes had invented empty corners above tilted panels and beside rotated rows, sending dimension lines far away or below ground. The true hull is shared by the interactive overlay and SVG figures.
- Dimension lines retain their projected endpoint separation and direction, choose an outward side that fits the viewport, and stay inside the drawing. Short/edge-on projections use point leaders instead of overlapping witness lines; collapsed angular projections likewise use a point label with explanatory SVG titles. Leaders connect to the nearest label edge, labels can shift locally to avoid each other, and every label is drawn after all linework. Figure framing includes annotation geometry and reserves its heading area.
- First-table and table-gap witnesses now follow the actual first-row shift in checkerboard layouts. Extra-aisle witnesses start after the regular row pitch and end at the next row centre. Single-row counts use a point leader; incomplete groups and nonexistent single-table/module gaps remain contextual rather than drawing implied spans. Fixed vertical/pergola racks omit the irrelevant default tilt callout; focusing an applicable input retains its explanation. Combined cropping-plus-aisle witnesses retain their explicit C + A value.
- Verification: 114 tests pass. New regressions check true hull extrema, physical witness lengths and positions across all five rack types and both module orientations, edge-on/offscreen handling, and 270 default layout combinations (five engineering stages, three rack types, three projections, three azimuths, desktop/mobile sizes). All labels and dimension lines fit their test viewports without label overlap. Browser inspection covered ortho, plan, profile, grouped aisles, single-row fields and checkerboard arrays; live ortho input focus preserved the camera. Production build, formatting and frozen-archive verification pass. The installed Radiance harness again reports zero mismatches over 51,100 rays / 10 cases; scientific geometry and irradiance algorithms are unchanged.

## User-defined within-cell sampling (September 15)

- Irradiance now has **Samples per grid cell**, accepting every integer from 1 to 9 and defaulting to 1. One preserves the existing centre-point calculation. Higher counts estimate irradiation and DLI means using equally weighted centres of equal-area subrectangles; 4 and 9 use regular 2 × 2 and 3 × 3 patterns. Non-square counts use proportional-height rows so each sample represents the same area. The grid, receiver coordinates, sensor locations and crop boundaries are unchanged.
- Direct and diffuse integration use the same sample locations for both opaque bitsets and bifacial transmission counts. Samples inside supports remain blocked and contribute zero; finite sampling can still miss small shadows. Samples are traced in separate batches, keeping peak visibility-matrix size at the one-sample size. Total ray work and potential cache storage increase with sample count. The same backend interface serves CPU and WebGPU; this change does not add hardware GPU validation.
- Sampling changes invalidate results and period checkpoints. Cache identities include the count, pattern revision and offset index. Older studies/results default to one sample and retain their previous analysis/cache keys. Daily and period results record `samplesPerCell`, and imports validate it. Browser saves and project packages retain the setting; tooltips distinguish point samples, estimated means and uniform control references. Methods, CSV and figure provenance report the sampling definition.
- Verification: 110 tests pass, including every allowed count, open-field energy/PPFD conservation, rotated unequal cells, independently shifted CPU reference solves for opaque and transmitting modules, cache isolation, the support-centre reproduction, period/checkpoint aggregation, package round trips and UI invalidation. Production build, formatting and the frozen-archive check pass. The installed Radiance harness again reports zero mismatches over 51,100 rays / 10 cases; it remains a first-hit occlusion comparison, not independent spatial-average or field validation.

## Restored grid defaults and zero receiver samples (September 15)

- Fixed the browser restore path that kept old uniform/coarse receiver settings active after the row-centred default was introduced. Unversioned browser preferences adopt nine cells per row-centre gap and 1 m nominal along-row spacing once. Weather, geometry and sky/time settings are retained. A browser-only revision marker preserves later explicit custom/preview choices; project-file imports preserve their original grids and numerical results.
- Reproduced an exact zero with a 0.2 m-high receiver point inside a vertical rack's 0.12 m support. The point remains zero at both 145 and 577 sky patches, while nearby points receive light. Row-aligned sampling avoids that collision in the regression fixture. The screenshot's matching pattern is consistent with support intersections; its exact study/weather record was not available to reproduce the specific receiver.
- Receiver tooltips identify cell-centre point samples and explain zero readings on hover. A receiver inside an opaque support is enclosed. Row alignment reduces these collisions but does not guarantee their absence for every geometry, receiver height or custom cell count. The subsequent sampling option above adds estimated cell means; explicit exclusion of solid-interior samples remains unimplemented.
- Verification: 106 tests pass, including browser-default migration/reload/import preservation, the simulated-DOM restore workflow, and the support-intersection reproduction. Production build, formatting and frozen-archive verification pass. The installed Radiance oracle again reports zero occlusion mismatches across 51,100 rays in 10 cases; its independent-validation scope is unchanged.

## Row-centred receiver grid and quiet diagnostics (September 15)

- New studies and Apply standard settings default to nine cells between adjacent PV row centre lines. Cell boundaries anchor to every row centre; wider access-aisle gaps still contain nine cells with larger across-row widths. Outer cells fit the original numerical footprint. Along-row spacing remains independent. Custom uniform spacing and the coarse-preview preset remain available; legacy imports retain their previous uniform grids and analysis keys.
- A shared grid description supplies exact row boundaries to sampling, Three.js/SVG tiles, hit testing, receiver outlines, physical sensors and crop beds. The control field uses the identical footprint/grid. Receiver and crop means, standard deviations and crop medians account for unequal cell areas; daily/period/project validation uses the same area weighting. Group copies/moves snap to compatible translations to preserve physical sizes and relationships across unequal cell widths.
- Grid alignment and cell count participate in numerical invalidation, visibility-cache keys and display refreshes. Exported JSON includes exact `yEdges`; CSV, figure captions and methods/provenance describe the alignment and spacing range. Crop lengths can represent sub-0.1 m receiver cells, while retaining the 100 m maximum.
- Routine backend, sky/time settings, GHI closure warnings and validation details are logged in one collapsed browser-console group. The long on-page model disclosure is removed; errors remain visible and exported provenance retains diagnostics. Console validation details use the checked-in Radiance record; report ray counts have been refreshed to 51,100 / 10 cases.
- Verification: 103 tests pass, including row/aisle alignment, rotated hit tests, unequal-area summaries, legacy key compatibility, cache isolation, field copies, package round trips and quiet UI checks. Production build, formatting and archive verification pass. The installed Radiance harness again reports zero occlusion mismatches across 51,100 rays / 10 cases; this is not independent sky, daily-energy, GPU or field validation. Browser checks verified the alignment selector, visible row-centred boundaries and changing nine to eighteen cells (21 × 39 to 21 × 78 in the example).

## Agrivoltaic and control layouts (September 14)

- Ten-stage workflow: Irradiance → Agrivoltaic → Control → Methods & export. Irradiance retains expanded inputs and excludes sensor/crop placement tools after calculation. Saved navigation migrates to the correct stages.
- First entry to Control copies sensor and crop layouts with distinct IDs into an independently editable, versioned `controlField`. Its receiver footprint, rotation and cell boundaries match Agrivoltaic; PV and PV land reservations are absent from the control drawing. Re-entry preserves control edits. Both layouts normalize after grid/geometry changes and remain excluded from browser autosave, as before; explicit JSON/ZIP exports preserve both.
- Control light uses the calculated unobstructed source reference uniformly: 100% relative sunlight and `openDli` (mean daily for period analyses), with the original measured/estimated PAR basis. No extra solver run is needed; stale agrivoltaic inputs invalidate both light displays. Without a matching result, control DLI is explicitly uncalculated.
- Shift-click or the item checklist selects multiple sensors and beds. Group duplication assigns unique IDs, retains dimensions and installation/crop metadata, and applies one configurable cell offset. Group movement and copies clamp their common translation at boundaries, preserving relationships; undo covers these edits.
- Reports include separate control instrument/crop tables, full-sun assumptions and a control plan figure. CSV rows identify their field; project packages contain both layouts and a separate control SVG. Control coordinates are local to each field centre; no surveyed control-site position is invented.
- Coarse preview changed three independent inputs; raising sky resolution alone could not refine its 3 m receiver spacing. Added a standard-settings reset (1 m / 577 patches / 10 minutes), current grid counts/actual spacing, and an explanation of spatial versus angular/temporal resolution.
- Verification: 97 tests pass, including two-field independence, boundary-preserving duplication, daily/period full-sun values, measured/estimated provenance, JSON/ZIP round trips, reports and the simulated-DOM workflow. Production build and frozen-archive verification pass. The installed Radiance oracle reports zero occlusion mismatches across 51,100 rays / 10 cases; independent sky, daily energy, GPU and field validation remain outside that comparison. Browser checks confirmed a PV-free control drawing and a standard reset refining the example grid from 7 × 12 to 21 × 35 cells.

## Weather download recovery (September 14)

- Daily and period downloads now share a 60-second deadline per HTTP request (including the response body), with one automatic retry after two seconds for timeouts, network failures and transient HTTP 408/500/502/503/504 errors. Progress identifies the retry and period chunk. HTTP 4xx rejection/rate limits and invalid weather data remain explicit failures; no synthetic or alternate-model fallback is introduced.
- Removed the old 30-second daily / five-minute whole-period abort timers. Explicit cancellation remains immediate, cancelled requests are not reused, and stale requests cannot overwrite weather status after source or site changes. Intentional cancellation no longer appears as a timeout error.
- All 92 tests and the production build pass. Six new download tests cover body timeouts, retry exhaustion, transient/permanent failures, cancellation, chunk retries and retained weather hashes/intervals. The simulated-DOM integration test also checks source-switch cancellation followed by a fresh same-site download. Formatting and archive verification pass.
- Live Tucson probes: the historical ERA5 endpoint returned no bytes within 45 seconds and again within 65 seconds; the forecast endpoint returned HTTP 200 in approximately one second. Historical service recovery was not verified. Retry handling cannot guarantee availability during an upstream or network outage; complete local weather uploads remain available.

## Racking layouts (version 0.4.1, September 12)

- Vertical racks use full-height outer posts and one shared upright between neighboring module columns, including table boundaries; the horizontal centre tube is removed. Selecting vertical racking defaults to bifacial construction. Both module faces share the cell appearance at module, rack, row and array scales; later module edits preserve an explicit construction override.
- Pergola racking offers aligned or checkerboard table rows. Checkerboard shifts alternating complete rows along the row by half the module-plus-gap spacing, aligning module centres with neighboring gap centres within corresponding tables. The finite array envelope, receiver grid, supports, solver geometry, publication figures and methods include the offset; single-row previews remain centred.
- Module gap accepts any finite nonnegative distance. Automatic clearances stay within the existing hardware limits; oversized designs retain overlap/ground/grid warnings rather than rejecting the gap through a derived field. Old studies default to aligned pergolas, and versioned analysis keys invalidate results computed with earlier geometry.
- All 86 tests pass, including five new racking/layout regressions. Production build, formatting and frozen-archive verification pass. In-app browser checks verified the vertical rack and row appearance, bifacial default, a 1.5 m gap, and checkerboard array plan. The opaque CPU/Radiance harness now covers 10 cases including checkerboard pergolas: 51,100 rays, zero visibility mismatches. This remains an occlusion comparison, not independent field or bifacial-energy validation.

## Implemented

- Frozen 480-file baseline, SHA-256 manifest, local write protection, archive CI check; original modeling brief and new AGENTS.md.
- New hierarchical version-2 Study schema (migrates version 1) with defaults, bounds, explicit unknown-version rejection and schema round trips.
- Plain-language help on every parameter, compatible defaults when selecting racking and after later module/table changes, persistent camera state, closer orthographic framing and receiver hover values.
- Ten-stage left-input/right-visualization workflow with separate Agrivoltaic and Control layout stages; orthographic Three.js scene; plan/profile/oblique views; responsive layout and keyboard controls.
- Fixed, single-axis, dual-axis, vertical bifacial and pergola geometry; table/row spacing, groups, supports and receiver extent.
- CPU MeshBVH and WebGPU WGSL BVH adapters; worker execution, progress, cancellation and CPU fallback.
- NOAA geometric sun; normalized Perez/Reinhart sky; daily aggregation by fixed/quantized tracker pose; bitsets and bounded IndexedDB cache.
- GHI closure and conservation; shared geometry for estimated or measured DLI; automatic Open-Meteo location/date downloads (ERA5 historical; recent/forecast explicitly labeled), imported CSV/EPW/TMY3 and optional synthetic sample; source/provenance hashing.
- Receiver-locked field layout with per-cell sensor packing/count badges, hover instrument lists, whole-cell crop plots, and actual receiver outlines in interactive/report views. Full array defaults to Orthographic.
- Mapbox address/place search with the archived public key, keyboard selection, cancellation of stale queries, atomic lat/lon updates and automatic weather refresh; longitude-estimated UTC offset is visibly labeled.
- Physical instruments including depth/orientation/treatment/replicate/logger/channel; map click placement and DLI-percentile placements.
- Crop plots and plot-level DLI statistics; grid results invalidated by scientific inputs but retained after instrument/plot edits.
- Canonical SVG figures with geometry, sensor/plot overlays, north arrow, scale, dimensions and a shared ground grid; high-resolution PNG; CSV, JSON and printable HTML methods report.
- Relative sunlight (received daily GHI fraction) in maps, receiver/plot summaries and exports; blue-low/yellow-high sunlight and DLI colors. Friendly table-count limits, explicit minimum-clearance hints and a repair action for saved layouts.
- GitHub Actions test/build/Pages deployment configuration.

## Measured verification

- 92 passing numerical/data/component tests cover schema round trips, rotated coordinates/area, finite geometry, geometric solar sanity, patch quadrature, Perez normalization, empty-field zero shade, finite shadows, energy conservation, PPFD conservation, PAR limiting values, tracker limits, stale results, weather rejection and download recovery, plot statistics, escaped publication output, sunlight/CSV consistency, report ground grids, large-assembly clearance bounds, daily tracking independent of preview tilt, rotated cell snapping, plot sample membership, JSON round trips, packed-marker bounds, Mapbox response handling, field dragging/resizing, controlled crop identities, vertical supports and checkerboard layouts.
- CPU versus Radiance: 51,100 identical rays, 10 geometric cases, **0 visibility mismatches**. Shared-weight irradiance differences were 0 W/m². Exact details: `radiance-validation.json`.
- Production build and local HTTP preview checked. All five racking types completed daily CPU calculations; canonical plan/profile/orthographic SVGs were independently rendered and visually inspected. Simulated-DOM integration tests cover a first-click calculation, dual-axis clearances through subsequent table edits, input help, and view selection through layer and sensor edits. A live Open-Meteo ERA5 response for the default site completed a 24-hour CPU calculation. A live Mapbox request with the public key and GitHub Pages referrer returned valid Tucson coordinates; simulated-DOM tests cover keyboard selection, full-array default view and receiver-locked sensor/crop additions without recalculation.
- September 10 review reran all 38 tests, the build, archive verification and the 45,990-ray Radiance harness successfully. Real in-app browser interaction completed a WebGPU daily calculation (5,123 receivers, 577 patches, 5.2 s), exercised cancellation and export generation, and inspected a 390 × 844 layout. That initial pass did not verify GPU/CPU parity or device loss; the repair verification below adds those checks. See `REVIEW-2026-09-10.md` for scope and reproductions.

## September 10 fixes (version 0.1.1)

- All eight recorded code defects are fixed: draft-based numeric commits, shared weather validation, bounded SVG extent reductions, complete display-fit bounds, effective rack parameters in methods, standalone provenance, retained source text plus canonical weather-input hashes, and constrained tracker pose bins.
- Added eleven regression tests. Valid Study version 1 records remain loadable; invalid saved records are preserved behind a recovery action with automatic saving paused; legacy weather without source text is explicitly labeled unavailable. New CSV/EPW/TMY3/Open-Meteo imports retain their original text and both hashes. Large snapshots can exceed localStorage capacity; JSON remains the portable record and the UI reports storage failures.
- Persistent renderer/camera/controls and reusable structural meshes; field metadata edits leave the drawing unchanged. Keyboard receiver inspector/placement, selected-state announcements, mobile jump links, visible sensor legends with depth-aware profile grouping, elapsed/work-weighted progress, an explicit coarse preview, and PR verification are implemented.
- Real browser checks verified `-0.15` keyboard entry, arrow-key receiver inspection, unchanged scene pixels during a notes edit, and a 390 px mobile drawing jump with no horizontal overflow. A generated publication figure and its provenance/legend were visually inspected; the PNG path produced and decoded a 3000 × 2304 image with the correct aspect ratio. Changed sample inputs and viewport were restored.
- Actual WebGPU versus CPU daily results matched at every receiver for five small rack designs (45 cells each). Persistent cache reuse in separate workers, actual device destruction, simulated allocation failure and simulated storage failure preserved results. Maximum observed Wh, DLI and relative-sunlight differences were zero. See `webgpu-validation.json`; this is one browser/runtime, not a broad hardware qualification.
- `validate:convergence` completed 44 sweeps / 132 CPU daily calculations: Tucson and Wageningen, June and December, fixed/single-axis/dual-axis racks, and sky/time/grid/pose settings. Largest receiver-area mean sunlight differences from each sweep's finest reference: sky 0.0511 percentage points; direct time 0.1878; grid 0.5932; pose bins 0.0016. These are small synthetic-array sensitivity measurements, not per-cell error bounds or publication acceptance criteria. See `convergence-validation.json`.
- Tests, production build, archive verification and Radiance comparison pass. Three.js core and renderer use separate production chunks; the build completes without the former size advisory. No deployment was performed.

## Land-use and publication refinement (version 0.1.2)

- Added explicit under-row no-crop width (U, default 1 m) and perimeter no-crop buffer (B, default 3 m), independent of the numerical receiver buffer R. Version-1 imports acquire defaults without moving existing layouts. Existing PV-edge setbacks S and centred maintenance lanes M are visibly delineated.
- Shared rotated ground-zone rectangles, screen-space hatching, engineering input-focus dimensions/angles/leaders, contextual notes for non-spatial settings, and central profile sections are implemented in interactive and publication views. Frame-edge setback placement includes module thickness. Camera framing survives focus changes and ordinary edits.
- Exact union areas prevent double counting; crop overlaps are reported without relocating plots. Planning edits preserve scientific results and receiver grids. Callout/metadata updates do not rebuild map-cell meshes.
- Publication parameters use paired four-column tables with a separate two-column methods/provenance appendix. Full Methods CSV values, standalone figure provenance, sensor keys and plot reservation overlaps are retained.
- All 54 tests pass; local browser checks cover input focus, default/rotated/zero-zone drawings, compact report tables and a 390 px mobile drawing without horizontal document overflow. Production preview, build and archive verification pass; the Radiance harness was rerun with zero mismatches on 45,990 rays. Cross-browser and physical print pagination remain release checks. Design definitions and verification details: `LAND-USE-AND-CALLOUTS.md`.

## Linked crop spacing and drawing clarity (version 0.1.3)

- Replaced independent setback/maintenance reservations with linked U/C/S inputs: U + C = pitch, S = (U − projected frame width) / 2. Signed setbacks permit cropping beneath PV. Cropping and non-cultivated zones share edges; group aisles add cropping width. Imports preserve U, recompute dependent values and drop the obsolete maintenance field. These definitions supersede the version 0.1.2 description above.
- Numeric inputs publish valid typing and arrow steps immediately, including dependent fields. Incomplete drafts remain editable; a regression test covers returning to a field after another linked field changes it.
- Removed the title icon. Engineering dimension lines/labels clear the module and default framing includes width in plan view. Labels avoid the hardware envelope and adjacent labels. Ground overlays now use depth-tested Three.js geometry beneath modules/racking.
- Added clickable layer legends and an array panel-opacity slider. Irradiance has no callouts and completed results initially hide ground/instrument/plot overlays. Display changes retain camera framing and scientific results. Standalone exports respect visibility/opacity; report light figures omit ground annotations while compact four-column geometry/methods tables retain all parameters.
- All 58 tests pass, including module label placement across projections and desktop/mobile sizes, linked crop spacing, opacity/depth behavior and post-solve interaction. Live browser checks inspected a 154-receiver synthetic CPU light map, restored cropping layers, opaque/translucent modules, default module plan dimensions and a 390 × 844 drawing without horizontal overflow. Production build, formatting and archive verification pass. The Radiance harness was rerun: 45,990 rays, zero visibility mismatches. Fixtures are available at `validation/display.html` and `validation/layout.html`; they do not alter saved studies.

## Interactive field workspace and crop identities (version 0.2.0)

- A successful irradiance calculation collapses the inputs to an expandable icon rail and exposes a wider field-layout workspace. Sensor/crop stages use the same workspace. The toolbar supports drag-from-palette or click placement, snapped item movement, four-corner bed resizing, Find item and 40-state session Undo. Profile retains coordinate editing; Top-down and oblique support planar dragging.
- Selected items open editable cards with complete sensor installation metadata or bed identity/layout/cultivar/notes. Apply/Cancel, undoable deletion, keyboard movement/resizing, pointer cancellation and focus restoration are implemented. A mobile bottom sheet keeps action buttons visible while its fields scroll. Field edits retain the irradiance result, camera and heatmap meshes and never enter scientific occluders.
- Added 426 predefined crop choices representing 309 accepted GBIF botanical identities, seeded from the FAO crop list and supplemented with agricultural crops/aliases. Realtime common/botanical-name search requires an offered selection. Known legacy names normalize to canonical records; unresolved imported names remain visible and require selection rather than receiving a guessed identity. The application bundles a dated taxonomy snapshot and works without live taxonomy requests.
- Each bed carries its common/botanical/scientific name, family, cultivar, crop-choice ID, GBIF record link/key and catalog version into JSON/CSV and compact publication tables. Botanical epithets are italicized separately from authorship. The methods appendix includes the GBIF citation and CC BY 4.0 attribution. Catalog generation and raw exact-match evidence are checked in. Definitions, sources and interaction details: [Field layout workspace](FIELD-LAYOUT-WORKSPACE.md).
- All 65 tests pass. Real browser checks completed a WebGPU daily calculation (577 patches, 5.3 s), confirmed automatic collapse, moved a sensor without changing height, resized a bed from a fixed opposite corner with updated statistics, selected Tomato through its botanical name, and dragged a sensor from the toolbar. Undo restored the original three-sensor/three-bed layout. A 390 × 844 viewport had no horizontal overflow; the corrected mobile card retained visible action buttons. A generated three-bed publication report was visually inspected, including its four-column identity table and six-column layout/light table. No browser console errors were recorded in that session.
- Production build, formatting and archive verification pass. The taxonomy data use a separate production chunk; the build has no chunk-size advisory. The Radiance harness was rerun with zero visibility mismatches across 45,990 rays. No deployment was performed. Physical touch hardware, cross-browser accessibility and print pagination remain broader release checks.

## Consolidated layout and clearer field IDs (version 0.2.1)

- Sensors and crop beds share one Field layout stage and the same expanded controls. The workflow is Irradiance → Field layout → Methods & export, with nine stages overall. Progress, navigation, placement modes and export controls use the revised sequence. Legacy saved sensor/crop navigation migrates to Field layout; legacy publication navigation stays at Methods & export.
- Removed engineering dimensions, angle/row callouts and numbered sensor leaders from Methods & export and all report figures. Field-layout figure exports also omit engineering callouts. Geometry-input stages retain their instructional dimensions; report parameter tables, scale, north and provenance remain available.
- Shared 12 px, white-backed ID tags replace unspaced names. Sensor tags prefer locations beside their dots; bed tags prefer bed edges. Tags avoid each other and diagram boundaries; selected items get priority. Crowded layouts omit excess tags with an explicit indication and retain every ID in Find item, report tables or standalone figure keys. Profile tags support item selection without planar dragging. Compact report figures reference field tables instead of repeating long sensor legends.
- All 68 tests pass, including navigation migration, dense/mobile label spacing, complete field identification, and callout-free report figures across plan/profile/oblique views. Live browser checks completed a 577-patch WebGPU calculation in 5.3 s, followed the merged post-solve sequence, verified absent Methods callouts, inspected six sensor/bed tags on desktop and a 390 × 844 screen, and opened the correct sensor through its tag. No study layout edits were made during this check. Browser console errors were absent. Generated publication plan/profile figures were visually inspected; physical print and cross-browser QA remain broader release checks.
- Production build, formatting and frozen archive verification pass. No scientific occlusion or irradiation algorithms changed and no deployment was performed.

## Portable project supplements (version 0.3.0)

- Header Open project / Export project and Methods & export produce a single standard `.agrivoltaic.zip`: authoritative editable Project JSON, matching complete receiver results, compact standalone HTML report, CSV tables, SVG figures, weather intervals/source text when retained, provenance, README and a SHA-256 manifest. Draft plans remain exportable with missing results/source records explicitly disclosed. No runtime dependency or server was added.
- A cancellable worker builds and validates packages. Import checks ZIP structure/CRC, all manifest file hashes, project content hash, Study schema and retained weather before presenting a replacement preview with backup/cancel options. Raw/previous Study JSON remains supported. Matching saved results retain their original backend/version/timestamp; stale or inconsistent results are excluded while the valid design remains usable. Imported weather snapshots stay pinned until explicitly refreshed or relevant inputs change.
- All 73 tests pass, including complete plan/weather/botanical/result round trips, independent installation depth, standalone/legacy JSON, corrupted checksums, stale/nonfinite/mispositioned results, ZIP STORE/DEFLATE, unsafe paths and decompression limits. The component integration test verifies preview cancellation, committed import, retained automatic-weather snapshots and restoration without starting the solver.
- Real in-app browser verification exported and reopened a 250-module, three-sensor, three-bed project with 5,123 WebGPU receiver results (577 patches; original calculation 5.3 s). Its 13-file ZIP was 724,277 bytes; this legacy weather record lacked original source text, which the package discloses. A separate full-source automated fixture verifies all 14 files. Python's independent ZIP reader validated the downloaded archive and every manifest SHA-256. Desktop and 390 × 844 import previews and the extracted report's compact tables were visually inspected; restored results and the pinned-weather message were confirmed. Browser console errors were absent.
- Production build, formatting and frozen archive verification pass (480 files, zero changes). The Radiance harness was rerun with zero visibility mismatches across 45,990 rays. Package versioning, contents, checksum encoding, browser fallback and limits are documented in [Project packages](PROJECT-PACKAGES.md). No scientific occlusion/irradiation algorithm changed and no deployment was performed. Cross-browser compression/file-picker behavior and physical print pagination remain broader release checks.

## Session-only layouts and browser capacity assessment (September 11)

- Browser autosave now excludes all sensor and crop-bed records on both read and write. Older valid autosaves no longer restore field layouts, and successful saving removes the old `fieldwork-study-v1` key. Geometry/site/weather settings and navigation still persist. Explicit project exports/imports retain complete layouts/results. Invalid saved-study recovery remains available with autosave paused. Header/footer copy explains that exporting is required to save field layouts.
- The component integration test now covers legacy autosave cleanup, adding sensors/beds without persisting them, and explicit project import restoring them in-session without recalculation. A real browser check added S-01 and P-01, reloaded, and confirmed both were absent while the Field layout stage and orthographic view remained selected. No browser warnings/errors were observed.
- Added a repeatable local capacity fixture and downloaded [raw performance evidence](browser-performance.json): 18 completed daily solves, two CPU cases stopped at the 30-second test budget, and three hardware-only drawing probes. Fixed WebGPU cases covered 250–48,000 modules and 5,040–19,152 receivers. The largest cold solve took 3.309 s on this browser; CPU cases at 10,000 and 48,000 modules exceeded the budget. This is one reported Apple M2 Pro/Chromium environment and does not establish universal limits, large-scale parity, peak memory, annual runtimes or full-UI capacity. Methodology and limitations: [Browser performance](BROWSER-PERFORMANCE.md).
- Assessed internal cell gaps/transmission and day/season/year analysis in [Extension assessment](EXTENSION-ASSESSMENT.md). At that point these were proposed extensions; version 0.4.0 implementation is recorded below. All 73 tests, production build and frozen archive verification pass; the installed Radiance harness again reports zero mismatches across 45,990 rays. No deployment was performed.

## Bifacial transmission and period analysis (version 0.4.0)

- Study schema 2 migrates version-1 designs. Bifacial selection enables internal X/Y cell gaps, cell counts, opaque perimeter and broadband/PAR laminate transmission. Fixed outer dimensions determine fitted cells and linked effective transmission. CPU and WebGPU count intersected module envelopes, attenuate each once and retain opaque support blocking. The module preview shows cell spacing; methods clearly disclose the area-averaged approximation.
- Single day, inclusive seasonal month range and calendar year now calculate every selected date. Leap days, cross-year seasons, chunked weather download/import, complete coverage checks, progress and same-session checkpoint resume are implemented. Period totals, energy-weighted relative sunlight and mean daily DLI are consistently labeled; projects retain aggregate receivers, full period weather and daily/monthly area summaries.
- All 81 tests pass. Actual transmitting CPU/WebGPU results match at every receiver across five small rack designs. A full 366-day browser CPU fixture completes and validates; a live 96-module February WebGPU run completes in 1.3 s. Seasonal project ZIP CRC/hashes were independently checked and its report visually inspected. A 32-ray Radiance scalar-transmission comparison has maximum difference 1.11e−16; the original 45,990-ray opaque comparison still has zero mismatches. Production build and frozen archive verification pass. No deployment was performed.
- Behavior, measured evidence, memory limits and remaining scientific limitations: [Transmission and periods](TRANSMISSION-AND-PERIODS.md). These checks do not establish universal annual browser capacity or physical field accuracy.

## Bifacial monthly/annual browser capacity (September 11)

- Added a repeatable, isolated-origin period benchmark using the production worker calculation, daily checkpoints and bounded memory/IndexedDB cache. Sixteen sequential runs covered June 2024 and leap-year 2024 with bifacial internal-gap transmission: twelve completed and four annual runs were deliberately stopped at 120 seconds. No production solver or UI behavior changed in this task.
- At 96 modules / 735 receivers, CPU month/year times were 1.42 / 12.85 s, versus WebGPU 4.11 / 43.91 s. At 250 modules / 5,040 receivers, the month favored WebGPU (37.38 s versus CPU 83.25 s); annual runs reached 171 GPU days versus 45 CPU days by the time limit. A 48,000-module / 19,152-receiver WebGPU month finished in 114.30 s. These are one-browser worker timings, not full UI or universal capacity claims.
- The small monthly CPU/GPU comparison matched exactly; annual maximum relative-sunlight difference was 0.000271 percentage points. Cache counters confirm that transmitting matrices over 4 MiB are recomputed across days. The minimal page remained responsive during the maximum-array monthly worker calculation. Full findings, timing table, evidence scope and reproduction: [Bifacial period performance](BIFACIAL-PERIOD-PERFORMANCE.md).

## Remaining scientific release gates

1. Extend the passing browser GPU/CPU and failure checks to representative large arrays, additional devices/browsers and actual memory-pressure limits. The September 11 timing probes cover large fixed-array GPU solves, but do not compare every receiver with CPU or identify the WebGPU adapter independently; the drawing probe reports an Apple M2 Pro WebGL renderer.
2. Extend the September 20 independent pvlib/Radiance sun, sky and daily comparisons to measured field data and independent PAR partition/conversion calibration; resolve the oracle-rejected low-sun case. Source timing and PAR partition remain shared in the daily comparison.
3. Extend the recorded sky/grid/time/pose sweeps to representative field geometries and measured weather; establish per-cell and publication acceptance tolerances. Existing sweeps report sensitivity only.
4. Browser interaction/accessibility/print-layout QA across desktop, mobile and Safari/Firefox/Chromium.
5. Independently validate transmitting-module optics and period irradiation against measured or independently integrated cases; current scalar Radiance transmission checks are deliberately narrow.

## Deliberate v1 simplifications

Horizontal receivers, flat terrain, square posts, opaque monofacial boxes or area-averaged bifacial gap transmission, idealized rack structures, no frame-specific optical material model, no reflected radiation or plant shading. Dual-axis tables are rectangular and yaw about their centres. Rectangular crop plots align with receiver cells and the array axes. No arbitrary plot polygons, automatic row-relative replicate generator, electrical yield solver, resolved cell-gap sunflecks, or multi-bounce optics. Site elevation is metadata rather than an atmospheric correction. JSON exports are the portable research record; local browser storage is a convenience.

These limitations must remain visible in methods documentation. Building/exporting a figure does not imply scientific validation.
